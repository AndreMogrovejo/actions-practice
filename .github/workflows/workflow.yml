name: Workflow

on: [pull_request]

jobs:
    Explore-Github-Actions:
        runs-on: ubuntu-latest

        steps:
            - run: npm install
            - run: yarn check-types
            - run: yarn lint
            - run: yarn test

